%h1 Edit a Post
= simple_form_for(@post, html: { class: 'form-horizontal' }) do |f|
    = f.input :title
    = f.input :body
    = f.button :submit
-# the form coding can be in partial view and then render here _form.html.haml 
-# = render "form"
%small= link_to "Cancel", post_path(@post)